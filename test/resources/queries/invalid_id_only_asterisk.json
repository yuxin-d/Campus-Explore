{
	"errorMsg": "The result is too big. Only queries with a maximum of 5000 results are supported.",
	"title": "invalid_only_asterisk",
	"input": {
		"WHERE": {
			"IS": {
				"courses_id": "*"
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_dept",
				"courses_id"
			],
			"ORDER": "courses_id"
		}
	},
	"errorExpected": true,
	"with": "ResultTooLargeError"
}
